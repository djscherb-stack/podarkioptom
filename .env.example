# Локально: скопируйте в .env и подставьте те же логин/пароль, что на сервере в интернете.
# Файл .env не попадает в git (безопасно).

ANALYTICS_USER=admin
ANALYTICS_PASSWORD=logos22

# ИИ-аналитика за день (оценка выработки, тренды, вопросы для руководителей)
# Без OPENAI_API_KEY блок «ИИ-аналитика» покажет подсказку о настройке.
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
